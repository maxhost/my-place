# Decisiones de arquitectura (ADR)

Registro de decisiones arquitectónicas con fecha, alternativas rechazadas y consecuencias. Una ADR es histórica: no se edita, se supersede con una nueva.

- [0001 — Auth: OIDC IdP propio, identidad separada y custom domains](0001-auth-oidc-custom-domains.md) — 2026-05-15
- [0002 — Roles owner/miembro, reconocimiento de pertenencia, ciclo de vida del handle](0002-roles-gamificacion-handle.md) — 2026-05-16
- [0003 — Lifecycle de cuenta y de place; tombstone; reemplazo de los 365 días](0003-lifecycle-cuenta-place-tombstone.md) — 2026-05-16
- [0004 — Capa de acceso a datos: Drizzle ORM](0004-acceso-datos-drizzle.md) — 2026-05-16
- [0005 — Onboarding del owner: creación de place y cuenta](0005-onboarding-creacion-place.md) — 2026-05-16
- [0006 — Provisión de `app_user`, RLS base y modelo rol/JWT](0006-provision-appuser-rls-base-rol-jwt.md) — 2026-05-16
- [0007 — Ajuste: el LLM del onboarding no propone horario; horario default](0007-ajuste-llm-horario-default.md) — 2026-05-16
- [0008 — Dos vías de entrada: CTA (place-first) vs "Acceso" (login form, account-first)](0008-dos-vias-de-entrada.md) — 2026-05-16
- [0009 — Cierre de los sub-puntos abiertos de ADR-0008](0009-cierre-subpuntos-adr-0008.md) — 2026-05-16
- [0010 — RLS por-operación + invitación solo por token-link](0010-rls-por-operacion-invitacion-token-link.md) — 2026-05-17
- [0011 — Función de identidad RLS propia (`app.current_user_id()`)](0011-funcion-identidad-rls-propia.md) — 2026-05-17
- [0012 — Creación de place vía función `SECURITY DEFINER`; INSERT denegado por RLS](0012-creacion-place-via-funcion-definer.md) — 2026-05-17
- [0013 — Cambio de stack: Next.js 15 → 16 (prerequisito de S4)](0013-upgrade-next-16.md) — 2026-05-17
- [0014 — Split del slice `onboarding` en `place-creation` + `access`](0014-split-onboarding-place-creation-access.md) — 2026-05-18
- [0015 — Extraer la asistencia LLM a un slice propio `style-assist`](0015-extraer-slice-style-assist.md) — 2026-05-18
- [0016 — Extraer la UI del wizard a un slice propio `place-wizard`](0016-extraer-slice-place-wizard.md) — 2026-05-18
- [0017 — Aprovisionamiento de entornos por migraciones; el deploy las corre; Neon branches efímeras](0017-aprovisionamiento-entornos-por-migraciones.md) — 2026-05-18
- [0018 — Adquisición del JWT de Neon Auth (`auth.token()`) y place-first two-phase](0018-jwt-neon-auth-y-place-first-two-phase.md) — 2026-05-19 · cierra TBD de 0006, corrige 0005 §1 / 0008 §2
- [0019 — `style-assist` también es dueño de su UI glue (hook + isla + labels contract)](0019-style-assist-owns-llm-ui-glue.md) — 2026-05-20 · refina 0015 · cierra deuda de slice place-wizard >1500
- [0020 — Pausa de la asistencia LLM propose-only en el onboarding del MVP](0020-pausa-asistencia-llm-onboarding.md) — 2026-05-19 · pausa parcial 0005 §5 / 0007 / 0019 · refina 0015 · también retira el campo "Descripción" del wizard
- [0021 — Patrón canónico para member-read: extender `_sel` con `OR exists(membership activa)`](0021-rls-member-read-pattern.md) — 2026-05-19 · refina 0010 §1 (cierra TBD de member-read) · habilita el Hub (`docs/features/inbox/`)
- [0022 — Locale propio del place: `place.default_locale` editable + 6 locales operativos en toda la app](0022-locale-del-place.md) — 2026-05-20 · cierra promesa de `multi-tenancy.md:14` · habilita el feature settings (`docs/features/settings/`)
- [0023 — App Shell agnóstico extraído a `shared/ui/app-shell` (consumido por `nav-hub` y `nav-place`)](0023-app-shell-agnostico-shared-ui.md) — 2026-05-20 · refina ADR-0014/0015/0016 · habilita S4 del feature settings
- [0024 — Fallback runtime deep-merge entre `defaultLocale.json` y `{locale}.json`; `check-translations` informativo](0024-i18n-fallback-deep-merge.md) — 2026-05-20 · habilita S1 del feature settings · cierra gap latente `routing.ts.locales` 4 declarados vs 1 archivo real
- [0025 — Sidebar agrupado en zonas conceptuales (`AppShell` acepta grupos) + `iconoir-react` como librería canónica de iconos](0025-sidebar-agrupado-iconoir.md) — 2026-05-21 · refina ADR-0023 (shell extiende contrato a `sidebarGroups` + `icon: ReactNode`) · habilita V1.1 del feature settings (4 grupos: Identidad/Estructura/Suscripción/Gestión · 9 items · 8 disabled como afordancia del roadmap)
- [0026 — Custom Domain V1: verificación lazy en page-load + lifecycle archived con partial unique](0026-custom-domain-v1-lazy-verification.md) — 2026-05-21 · refina ADR-0001 (la verificación V1 es lazy poll, no cron continuo) · habilita el feature custom-domain V1.1 (`docs/features/custom-domain/`) · `oauth_client_id` queda NULL hasta Feature C (ADR-0027 retroactiva)
- [0028 — Custom Domain: promoción a slice propio `src/features/custom-domain/` en S4 del plan V1](0028-custom-domain-slice-promotion.md) — 2026-05-21 · refina la asunción de S0 (Dominio como sub-feature de `place-settings`) · habilita que `place-settings` cumpla el cap LOC ≤1500 + alinea el grafo de dependencias con Features B (host routing) y C (OIDC SSO) que consumirán `custom-domain` directo · precedente de ADR-0014/0015/0016/0019
- [0029 — Custom Domain: chequear V6 `misconfigured` además de V9 `verified` (cierre falsa-positiva)](0029-custom-domain-v6-misconfigured-check.md) — 2026-05-22 · refina ADR-0026 (la lazy verification V1 era incompleta — solo V9 sticky, faltaba V6 dinámico) · bloqueante para Feature B (host routing) que rutearía dominios falsa-positivos
- [0030 — Custom Domain: split del slice por capa de operación (commands vs lazy poll) cuando el cap LOC se supera](0030-custom-domain-split-by-operation-layer.md) — 2026-05-22 · refina parcialmente ADR-0028 §"Alternativas rechazadas" #4 (revisita la decisión de no-sub-dividir con el nuevo contexto post ADR-0029) · habilita que `custom-domain` y `custom-domain-verification` cumplan ambos el cap LOC ≤1500
- [0031 — Custom Domain Routing V1: lookup `SECURITY DEFINER` + proxy async + auth-gate educativo](0031-custom-domain-routing-v1.md) — 2026-05-22 · refina ADR-0026 §5 (materializa el forward-compat de Feature B; cierra 3 decisiones que ADR-0026 no podía cerrar: shape jsonb del lookup, runtime Node confirmado, cookie cross-domain gap cubierto con gate UX V1) · habilita el host routing real de `mi-place.com → place` + slice nuevo `custom-domain-routing` + fix de 3 redirects hardcoded pre-existentes · difiere Feature C (OIDC SSO) + cache V2 + cron safety net (#103, importancia aumenta post-B)
- [0032 — Custom Domain SSO: Signed Ticket cross-domain (refina ADR-0001 §1, supersede §3, supersede ADR-0027 futura)](0032-custom-domain-sso-signed-ticket.md) — 2026-05-22 · refina ADR-0001 §1 (topología "dos mundos" se mantiene; el SSO interno NO es OIDC canónico sino Signed Ticket pattern — JWT ES256 short-lived emitido por apex y redeemed por custom domain) · supersede ADR-0001 §3 (no se provisiona OIDC client per dominio; `place_domain.oauth_client_id` queda NULL indefinidamente) · supersede ADR-0027 (futura, nunca escrita — el "script idempotente de provisioning retroactivo" anticipado por ADR-0026 §4 ya no aplica) · refina ADR-0031 §11 (Feature C cierra el gap con Signed Ticket en lugar de OIDC silent auth; `<AuthGateForCustomDomain>` locked + reusado como CTA fallback) · habilita silent SSO server-side sub-segundo desde custom domain (`nocodecompany.co/settings` con owner autenticado en apex → 4 redirects 302 + sesión local `__Host-place_sso_session`) + módulo nuevo `src/shared/lib/sso/` (sub-cap LOC 800 propio) + 4 endpoints nuevos + migration 0011 (`app.consume_sso_jti` SECURITY DEFINER anti-replay) + componente `<SsoFallbackPanel>` cuando falla · difiere V2 multi-key rotation (zero-downtime) + V2 rate limiting `/api/auth/sso-issue` + V2 logout cascade + cron safety net (#103, importancia AUMENTA post-C de nuevo)
- [0033 — Apex login honra `?returnTo` para cerrar el cold-start SSO desde custom domain (Feature C S11.3)](0033-apex-login-honors-returnto.md) — 2026-05-23 · refina ADR-0032 §2 step 2 (cierra el contrato del lado consumer del `returnTo` que `sso-issue` ya emitía correctamente desde `redirectToApexLogin`) · habilita que el cold-start SSO M1 (visitor anónimo en custom domain sin sesión apex previa) aterrice transparentemente en el path solicitado post-login en lugar de descartarlo y mandar al Hub canónico · introduce helper PURE `validateLoginReturnTo` en `src/shared/lib/sso/` con allowlist explícito (`/api/auth/sso-{issue,init}` + relative paths) + same-registrable-domain HTTPS para absolute URLs + reject de open-redirect vectors (precedente same-registrable-domain policy de S11.1 `sso-jwks-fetcher`) · backwards-compat: flows pre-Feature-C sin returnTo siguen al Hub canónico hardcoded
- [0034 — Zone-aware DB helper para Server Actions agnósticas de zona (Feature C S11.2 retroactiva)](0034-zone-aware-db-helper.md) — 2026-05-23 (instanciación en código) · 2026-05-24 (ADR redactada en DS S5) · refina parcialmente ADR-0032 §6 (formaliza el dispatch layer que aprovecha la continuidad RLS del `sub` cross-zona) · sin supersedir nada (los 2 primitivos `getAuthenticatedDb` Feature A y `getAuthenticatedDbWithVerifier` Feature C S4 quedan intactos como capas de abajo) · habilita que cualquier Server Action o RSC con efectos owner-only se ejecute correctamente tanto en apex como en custom domain sin saber a priori en qué zona corre · introduce helper coordinador `getAuthenticatedDbForRequest(fn)` con seam-split (PURE `decideAuthBranch` en `src/shared/lib/db-for-request-decision.ts` + integrator async en `src/shared/lib/db-for-request.ts`, fuera de `shared/lib/sso/` porque es ortogonal al SSO) · 4 Server Actions migradas mecánicamente (`update-default-locale`, `register-custom-domain`, `archive-custom-domain`, `get-custom-domain-status`) · invariante going-forward documentada en `docs/gotchas/zone-aware-db-cookie-source.md`
- [0035 — `place_ownership` multi-owner V1: WORM-via-DEFINER + founder no-delete + transferencia 1:1](0035-place-ownership-multi-owner-v1.md) — 2026-05-24 · refina ADR-0002 §1 (multi-owner como invariante explícita), ADR-0003 §1 (exención owner literal post-multi-owner), ADR-0012 §2 (policy `place_ownership` reescrita via helper `app.current_user_owns_place` `SECURITY DEFINER` no-recursivo) y §3 (`app.create_place` setea `founder_user_id`) · habilita refactor RLS + 4 funciones DEFINER (`current_user_owns_place`/`elevate_to_owner`/`revoke_ownership`/`transfer_founder_ownership`) + migrations 0012-0016 · difiere wrappers TS + UI + Server Actions + invitation flow co-owner + auto-revoke (V1.1+)
- [0036 — Bio contextual del miembro: `headline` per place](0036-member-bio-contextual.md) — 2026-05-24 · refina canon `docs/ontologia/miembros.md` previo "no hay bio escrita por el miembro" (la identidad-por-contribución sigue siendo el corazón; el headline es slot opcional ≤280 chars per place, vive en `membership.headline`, edita sólo el propio miembro) · habilita Feature E (members slice V1) §"perfil contextual con headline"
- [0037 — Cupo configurable de invitaciones por miembro (V1 schema-only)](0037-member-invite-quota.md) — 2026-05-24 · refina ADR-0010 §2 (originalmente "owner crea invitación" como propiedad estática; ahora introduce `place.member_invite_quota` configurable + owner exento, V1 ships schema-only DEFAULT 0 = comportamiento histórico, V2+ agrega UI + gate por cupo + counter `membership.invitations_used`) · habilita Feature E (members slice V1) §"invitation flow owner-only V1 + schema-ready V2"
- [0038 — Member peer-read sobre `app_user`: extender ADR-0021 al 3er sujeto del trio](0038-rls-app-user-member-peer-read.md) — 2026-05-25 · refina ADR-0021 (extiende patrón "owner OR member-self" al 3er sujeto del trio `place`/`membership`/`app_user`) · cierra gap detectado en Feature E S6 (loadMembers/loadPendingInvitations imposibles sin policy peer-read sobre `app_user`) · habilita member-list UI con identidad universal (display_name/handle/avatar_url) JOIN-eable cross-user dentro del mismo place
- [0039 — Enforcement de slice boundaries vía eslint built-in `no-restricted-imports`](0039-eslint-slice-boundaries-enforcement.md) — 2026-05-25 · cierra drift `architecture.md:19` (afirmaba "Enforzadas por eslint con `no-restricted-paths`" — el `eslint.config.mjs` real no tenía rule alguna pre-ADR; ~7 meses de drift detectado en gap-scan pre-S10.6) · adopta Path B (regla strict de `/public` + escape hatch puntual `eslint-disable-next-line` con rationale completo) · única violación pre-ADR son 2 occurrences en `custom-domain-verification` deep-importando `custom-domain/types/custom-domain` por workaround Vitest+next/headers (ya documentadas en `_v6-helpers.ts:6-10` + ADR-0030) · habilita going-forward enforcement automático del paradigma vertical-slice (CI bloquea futuras violaciones) · rechaza Path A (allow `types/**` cross-slice — relaja contrato por 1 caso real) y Path C (split `public.ts` en dos barrels — convención nueva sin precedente)
- [0040 — Rename slice `members-ownership/` → `place-ownership-actions/` (capability-named)](0040-rename-place-ownership-actions.md) — 2026-05-25 · primera de las 4 ADRs de refactor slice diet (S10.6-S10.9) post-S10.5 Plan B split · cambio puramente nominal (13 files, 0 LOC delta de lógica): el nombre original `members-ownership/` mapeaba la relación de consumo (qué slice consume) en lugar de la capability del slice (qué hace) · nombre canónico `place-ownership-actions/` ancla al schema slot `place_ownership` + es consumer-agnostic (preparado para V1.1+ con segundo consumer cross-slice) + el sufijo `-actions` desambigua del Feature D doc `place-ownership/` (DB-only) · rechaza `place-ownership/` (colisión doc Feature D), `ownership-actions/` (sin contexto schema), `place-ownership-{wrappers,commands}/` (jerga + convención nueva sin precedente) · write-back canonical decision §S8 line 606 plan-sesiones members (Original → Plan B → Rename evolution) · ADR-0039:16 NO se edita (inmutabilidad ADR)
- [0041 — Extraer slice `invitations/` desde `members/` (capability-named)](0041-extract-invitations-slice.md) — 2026-05-25 · segunda ADR del refactor slice diet (S10.6-S10.9) · primer split sustancial post-rename 0040: 12 files `git mv` (history preservada) + 3 files split-edit (types/schemas/schemas.test) + creates `invitations/{types,public}.ts` + nuevo `invitations/actions/_lib/{schemas,__tests__/schemas.test}.ts` · cohesión del slot DB `invitation` (migrations 0018-0019) exclusivamente — flujo asincrónico owner→invitee ortogonal a `membership` (`members/`) y `place_ownership` (`place-ownership-actions/`) · members 3765 → 2237 LOC (-1528 / -41%); invitations 1497 LOC (bajo cap 1500 post micro-cleanup -118 LOC doc redundante) · ADR-0028 §"Política a futuro" 3 criterios cumplidos (cap + migrations/spec propios + page S11 como 1er cross-slice consumer) · rechaza status quo (cap violado), subfolder `members/invitations/` (sin barrier ADR-0039), `members-invitations/` (anti-pattern ya corregido en 0040), `invitation/` singular + `place-invitations/` (convención + redundancia) · 0 cross-slice imports a actualizar (todas las refs eran intra-`members/`)
- [0042 — Extraer slice `member-profile/` desde `members/` (capability-named)](0042-extract-member-profile-slice.md) — 2026-05-25 · tercera ADR del refactor slice diet (S10.6-S10.9) · 2º split capability del slice padre, mismo playbook que 0041: 5 files `git mv` (history preservada) + 3 files split-edit (types/schemas/schemas.test) + creates `member-profile/{types,public,actions/_lib/schemas,actions/_lib/__tests__/schemas.test}.ts` · cohesión perfil contextual self-only del miembro en este place — asimetría caller==target vs owner→target (`removeMemberAction`) hace explícita la separación capability · nombre cubre V1 (`headline`) + reserva V1.1+ (avatar contextual + cualquier otro campo perfil-en-place) sin renombrar · members 2237 → 1678 LOC (-559 / -25%); member-profile 589 LOC (bajo cap) · members aún sobre cap por 178 LOC — pendiente S10.9 refactor `member-row-actions-menu` a page-level con render-prop (no es extracción) · rechaza status quo (cap + asimetría permisos invisible), incluir `<MembersList />` (mezcla listado-de-otros con edit-propio), `member-headline/` (data-shape-named: scope vencido al sumar avatar), subfolder `members/profile/` (sin barrier ADR-0039) · 0 cross-slice imports a actualizar
- [0043 — Mover `<MemberRowActionsMenu />` a page-level co-located + render-prop](0043-member-row-actions-menu-page-level.md) — 2026-05-25 · cierre del refactor slice diet (S10.6-S10.9): cuarta y última ADR · a diferencia de las 3 anteriores NO es extracción de slice — es restructuring del cómo se monta · 2 files `git mv` (menu + test, history preservada al 100%) de `src/features/members/ui/` → `src/app/(app)/place/[placeSlug]/settings/members/_components/` · refactor `<MembersList />` a render-prop puro: drop 5 props (actions/callerCtx/placeId/placeSlug/menuLabels) → agrega `renderRowActions?: (member) => ReactNode` opcional · razón: el menú ensambla 4 actions cruzando 2 slices (`members/` 1 + `place-ownership-actions/` 3) con un único page-consumer y sin DEFINER/spec propia ⇒ no califica como slice (ADR-0028 §"Política a futuro") y no encaja en ninguno de los 2 padres sin recrear acoplamiento · inaugura convención `app/.../_components/` (Next.js `_*` = directorio privado) para page-level glue code · members 1678 → **1074 LOC** (-604 / -36%, **426 bajo cap**; total slice diet S10.5-S10.9: 3765 → 1074, -71%) · rechaza status quo (gap LOC + no-pertenencia conceptual), nuevo slice `member-row-actions/` (viola ADR-0028 — sin DEFINER/spec/consumers cross-slice), refactor sin mover (no descomprime LOC), inline en page S11 (viola "app/ delgado" + pierde testabilidad) · ADRs 0040/0041/0042 NO se editan (inmutabilidad) — referencian state pre-S10.9
- [0045 — Invite signup CTA via `/login?mode=signup` (supersede ADR-0044 §D3 extensión de `/crear`)](0045-invite-signup-cta-via-login-mode-signup.md) — 2026-05-26 · supersede ADR-0044 §D3 (decision sólo del signup CTA path; §D1/D2/D4/D5/D6/D7 intactas) · refina ADR-0033 (ortogonalmente — el nuevo query param `?mode=signup` vive en page handler `/login`, no en el helper PURE `validateLoginReturnTo`) · habilita coherencia semántica CTA→form: invitee que clickea "Crear cuenta" en `/invite/{token}` aterriza en `/login` con tab signup pre-seleccionado (sin obligación de crear place propio que no quiere, mismo día que ADR-0044 — repivot pre-S5 sin código shipeado del `/crear` extension original) · introduce param query whitelist `?mode=login|signup` en `/login` + prop opcional `initialMode?: "login" | "signup"` en `<AccessFlow>` (additivo, default backwards-compat) + `useAccessForm({ initialMode })` · `/crear` y `place-wizard/` y `place-creation/` slices NO se tocan (explícito) · rechaza implementar ADR-0044 §D3 as-written (UX rota + scope cross-feature mayor + duplica capability ya en `/login`), signup CTA sin `?mode=signup` (rompe contrato semántico del label), página dedicada `/signup` (duplica `<AccessFlow>` sin ganar capability), `?signup=1` boolean (menor extensibilidad vs enum `mode`)
- [0044 — Invite Accept Flow V1.1: `/invite/[token]` consume DEFINER existente + sub-cap LOC bump invitations/ 1500→1800](0044-invite-accept-flow.md) — 2026-05-26 · refina ADR-0010 §2 (instancia el consumer del token-link capability — V1.1 formaliza el "click → preview → consent → accept" que ADR-0010 §2 enunciaba sin detallar a nivel page/route) · extiende ADR-0033 §"explicit allowlist" (agrega pattern `/invite/[token]` + same-registrable-domain absoluto al validator de `validateLoginReturnTo`) · refina ADR-0041 (sub-cap LOC bump 1500→1800 para `invitations/` — precedente ADR-0032 §5 con sub-cap 800 propio para `src/shared/lib/sso/`) · NO supersede nada · gap detectado en smoke E2E post-deploy Feature E V1 (`dpl_J5QcmBYmLQENi9yz8owK5j8eCt4N`, 2026-05-26): `GET /invite/{token}` retornaba 404 porque ni la ruta ni el page existían (S12 spec V1 asumió erróneamente "flow de Feature C" — Feature C es custom-domain-sso, NO signup/login) · habilita: ruta nueva zona-place `(app)/place/[placeSlug]/invite/[token]/page.tsx` (subdomain proxy con cross-place tampering check natural via `placeSlug` ↔ `invitation.place_id`) + Client panel `_components/invite-acceptance-panel.tsx` (state machine consent + 7 errors mapeados) + Server Action `acceptInvitationAction` (consume `app.accept_invitation` existente, sin migration nueva) + map de errores `_lib/map-accept-error.ts` (7 SQLSTATEs canónicos: 28000/P0002/P0005/P0006/P0007/P0008/P0009) + extensión `validateLoginReturnTo` allowlist + extensión `/crear` honra returnTo post-signup + i18n nuevo namespace `placeInvitation` × 6 locales · rechaza ruta `/[locale]/invite` zona marketing (pierde cross-place tampering + branding + locale del place + forward-compat custom domain), auto-accept en GET (rompe modelo capability + vulnerable preview-fetch bots), login redirect inmediato sin preview unauth (pierde transparencia + desperdicia `invitation_preview`), slice nuevo `invite-accept/` (viola ADR-0028 §"Política a futuro" — 3 criterios no se cumplen), auto-switch cuenta en P0008 (out of scope V1 — multi-account no soportado), quota V2 + place full unblock UI (scope ADR-0037 §V2 — V1.1 honra V1 schema-only)

> Los números de ADR se asignan al redactarse, no se reservan por adelantado. La ADR de comisión/pricing (referida en `docs/landingpage/`) tomará el número que corresponda cuando se cierre.
